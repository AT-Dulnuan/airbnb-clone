import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        margin: 20
    },
    textInput: {
        borderRadius: 50,
        color: '#121212'
    }
})

export default styles